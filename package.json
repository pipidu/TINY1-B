{
  "name": "tiny1-b",
  "version": "0.1.0",
  "description": "TINY1-B — a tiny starter web app (Express API + static frontend).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
